apiVersion: v1
kind: ResourceQuota

metadata:
  name: risk-prod-quota
  namespace: risk-prod

spec:
  hard:
    requests.cpu: "10"
    requests.memory: 20Gi
    limits.cpu: "20"
    limits.memory: 40Gi

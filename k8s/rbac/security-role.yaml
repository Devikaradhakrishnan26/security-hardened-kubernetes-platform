apiVersion: rbac.authorization.k8s.io/v1
kind: Role

metadata:
  name: security-role
  namespace: payments-prod

rules:
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["get","list","watch"]

- apiGroups: [""]
  resources: ["events"]
  verbs: ["get","list"]
